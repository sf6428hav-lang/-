# 网站 MVP 方案

## 1. 产品目标

做一个自用的小网站：粘贴一个短剧链接，网站自动读取视频内容，按指定格式**一比一还原视频剧本**。

- 使用者：自己 + 一位朋友，2 人使用
- 不做登录、不做付费
- 核心输入：抖音链接、红果短剧/漫剧链接
- 核心输出：与《在麻将声中写作业，我逆风翻盘了》一致的分场短剧剧本

## 2. 已验证的可行性结论

### 红果（已验证可用）

红果分享链接会返回一个 H5 页面，页面数据里带有：

- 剧名、简介、集数等元数据
- 可直接下载的 MP4 视频直链（测试《深宫恨》时返回约 6.1MB 的 MP4，无加密）

因此红果侧可以走通：分享链接 → 解析视频直链 → 下载视频 → 提取音频 → 语音转文字 → 整理成剧本。

### 抖音（采用开源解析工具）

直接访问抖音网页会被风控拦截，但可以借助开源解析工具拿到视频和字幕/文字：

- 首选 `yzfly/douyin-mcp-server`：下载抖音视频、提取音频并转成文字
- 备选 `Evil0ctal/Douyin_TikTok_Download_API`：成熟的抖音/TikTok/B站解析服务

该部分待落地时用真实链接做一次端到端验证。

## 3. 核心技术链路

1. 接收用户粘贴的分享链接
2. 识别平台（抖音 / 红果）
3. 解析链接，拿到视频直链和元数据
4. 下载视频，用 ffmpeg 提取音频
5. 语音识别（ASR）把音频转成带时间轴的文本
6. 用大模型把文本整理成《麻将声》格式的分场剧本
7. 输出 docx / txt，支持复制、重新生成、保存历史

## 4. 输出格式规范（参考样例）

以《在麻将声中写作业，我逆风翻盘了》为模板，单集结构如下：

- 标题
- 第 X 集
- 场次：如 `1-1 日 内 客厅`
- 人物：列出本场出场角色
- 动作/镜头提示：以 `▲` 开头，必要时使用特写、近景、闪回、蒙太奇
- 台词：`角色名（语气/动作）：台词`
- 旁白与内心：VO、OS
- 字幕/字卡：`▲【字幕：……】`

一比一还原时，以视频实际台词和画面动作为准，不新增虚构剧情。

## 5. MVP 范围

做：

- 单个链接 → 生成剧本 → 下载 docx/txt
- 简单保存生成历史
- 剧本模板可编辑（先支持《麻将声》格式）
- 支持抖音、红果两类链接

先不做：

- 登录、付费、多用户协作
- 批量导入、全集自动连续生成
- 复杂权限和后台管理

## 6. 待确认事项

- 抖音解析工具最终确认（`yzfly/douyin-mcp-server` 还是 `Douyin_TikTok_Download_API`）
- 语音识别服务的 API Key（推荐 SiliconFlow、火山引擎或 OpenAI Whisper 等）
- 部署方式：本地运行还是云服务器
- 剧本是否需要同时导出 docx 和 txt

## 7. 下一步

先搭一个最小验证原型，用已测的红果链接《深宫恨》跑通“链接 → 视频 → 音频 → 文本 → 剧本”的完整闭环，再接入抖音并补全网页界面。
