# 产品设计文档

## 产品概述

一个自用的短剧剧本生成工具，用户粘贴抖音或红果的分享链接，系统自动分析视频内容，按《麻将声》格式一比一还原输出分场剧本。

**使用者**：自己 + 一位朋友，共 2 人
**付费模式**：无，自备 Gemini API Key

---

## 核心流程

粘贴链接 → 解析视频直链 → 下载视频 → 交付 Gemini 3.1 Pro 多模态分析 → 输出《麻将声》格式剧本

### 支持平台

| 平台 | 方案 | 状态 |
|------|------|------|
| 红果短剧/漫剧 | 解析分享页 H5 数据，获取 MP4 直链（已验证可通） | 已确认 |
| 抖音 | 通过 yzfly/douyin-mcp-server 或 Douyin_TikTok_Download_API 获取视频 | 待验证 |

---

## 功能清单

### 1. 链接输入

- 单条输入：输入框 + 添加按钮，逐条加入生成队列
- 自动识别平台（抖音 / 红果 / 其他），显示对应标识
- 支持删除单条链接和清空全部

### 2. 批量生成

- 多条链接按顺序逐个生成
- 实时显示队列进度（状态圆点：等待/生成中/已完成/失败）
- 进度条展示整体完成百分比
- 全部完成后自动记录到历史

### 3. 剧本生成

- 调用 Gemini 3.1 Pro 多模态 API，直接分析视频画面和音频
- 内置提示词模板（《麻将声》格式），用户无需编辑
- 输出《麻将声》标准分场剧本格式
- 支持 docx 和 txt 两种格式下载

### 4. 生成历史

- 左侧边栏展示历史记录，按时间倒序排列
- 单条生成、批量生成均有记录
- 点击历史条目可查看对应的剧本内容

### 5. 设置

- API Base URL 配置项
- API Key 配置项
- 提示词模板预设（不可编辑，由开发者维护）

### 6. 后端（后续实现）

- Python FastAPI 后端
- 红果链接解析器（从 H5 页面提取 play_url）
- 抖音链接解析器（调用开源工具）
- Gemini 3.1 Pro API 调用
- python-docx 生成 docx 文件
- SQLite 或文件存储保存历史记录

---

## 界面布局

```
┌─────────────────────────────────────────────────┐
│  🎬 剧本生成器     [⚙️ 设置]                    │
├──────────────┬──────────────────────────────────┤
│  📎 添加短剧链接 │  📄 选择或生成剧本            │
│  ┌──────────┐ │  ┌──────────────────────┐      │
│  │ 输入框 + 添加│ │  │  剧本正文区域        │      │
│  └──────────┘ │  │                      │      │
│  ┌──────────┐ │  │                      │      │
│  │ 链接队列   │ │  │                      │      │
│  │ [抖音]xxx │ │  │                      │      │
│  │ [红果]xxx │ │  │                      │      │
│  └──────────┘ │  └──────────────────────┘      │
│  [🎬 开始生成]  │  [📄docx] [📃txt]              │
│  ──────────── │                                  │
│  📋 生成历史   │                                  │
│  深宫恨 第1集  │                                  │
│  深宫恨 第2集  │                                  │
└──────────────┴──────────────────────────────────┘
```

---

## 数据模型

### 生成记录

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| title | string | 标题 |
| type | string | single / batch |
| links | string[] | 原始链接列表 |
| output | string | 剧本内容 |
| created_at | datetime | 生成时间 |
| status | string | done / partial |

### 设置

| 字段 | 类型 | 说明 |
|------|------|------|
| api_base_url | string | API 中转站地址 |
| api_key | string | API 密钥 |
| prompt_template | string | 提示词模板（预设） |

---

## 待补充 / 后续迭代

- 登录与多用户
- 剧本模板的自定义切换
- 全集自动连续生成
- 搜索结果与过滤
- 服务器部署配置
